# 📚 Ebook Library - ห้องสมุดอิเล็กทรอนิกส์

ระบบห้องสมุดอิเล็กทรอนิกส์ที่ใช้ Google Sheets เป็นฐานข้อมูล และ Google Apps Script เป็น API

## 🎯 ฟีเจอร์

- ✅ แสดงรายการหนังสืออิเล็กทรอนิกส์
- ✅ ค้นหาหนังสือ (Real-time search with debounce)
- ✅ กรองตามหมวดหมู่
- ✅ Pagination (12 เล่มต่อหน้า)
- ✅ Modal แสดงรายละเอียด
- ✅ ดาวน์โหลดหนังสือ
- ✅ Responsive Design
- ✅ Keyboard Navigation (Arrow keys สำหรับ pagination)
- ✅ Dark Blue Theme สวยงาม
- ✅ Smooth Animations

## 📁 โครงสร้างไฟล์

```
ebook-library/
├── index.html          # หน้าเว็บหลัก
├── style.css           # Stylesheet
├── script.js           # JavaScript logic
├── apps-script.js      # Google Apps Script (สำหรับวางใน Google Sheets)
└── README.md           # คู่มือนี้
```

## 🚀 วิธีการติดตั้ง

### ขั้นตอนที่ 1: ตั้งค่า Google Sheets

1. สร้าง Google Sheet ใหม่หรือใช้ที่มีอยู่
2. ตั้งชื่อ Sheet Tab เป็น `ebook`
3. สร้างหัวตารางในแถวที่ 1:

| A | B | C | D | E | F |
|---|---|---|---|---|---|
| title | author | category | coverUrl | description | linkdownload |

4. เพิ่มข้อมูลหนังสือตั้งแต่แถวที่ 2 เป็นต้นไป

**ตัวอย่างข้อมูล:**

| title | author | category | coverUrl | description | linkdownload |
|-------|--------|----------|----------|-------------|--------------|
| การละหมาดตะฮายุต | อาจารย์ ABC | การละหมาด | https://example.com/cover1.jpg | คำอธิบายหนังสือ... | https://example.com/book1.pdf |
| พระองค์ทรงอยู่ใกล้ | อาจารย์ XYZ | การขอดุอาอฺ | https://example.com/cover2.jpg | คำอธิบายหนังสือ... | https://example.com/book2.pdf |

### ขั้นตอนที่ 2: ตั้งค่า Google Apps Script

1. ใน Google Sheet คลิก **Extensions** → **Apps Script**
2. ลบโค้ดเดิมออกทั้งหมด
3. คัดลอกโค้ดจากไฟล์ `apps-script.js` แล้ววาง
4. คลิก **Save** (💾)
5. คลิก **Deploy** → **New deployment**
6. คลิกไอคอน ⚙️ ข้างๆ "Select type" → เลือก **Web app**
7. ตั้งค่า:
   - **Description**: Ebook Library API (หรืออะไรก็ได้)
   - **Execute as**: Me (your-email@gmail.com)
   - **Who has access**: Anyone
8. คลิก **Deploy**
9. คลิก **Authorize access** → เลือก Google Account → **Allow**
10. **คัดลอก Web app URL** (จะมีหน้าตาประมาณนี้: `https://script.google.com/macros/s/AKfycby.../exec`)

### ขั้นตอนที่ 3: แก้ไข Frontend Code

1. เปิดไฟล์ `script.js`
2. แก้ไขบรรทัดที่ 3:

```javascript
// เปลี่ยนจาก
const APPS_SCRIPT_URL = 'YOUR_APPS_SCRIPT_URL_HERE';

// เป็น (ใส่ URL ที่คัดลอกมา)
const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycby.../exec';
```

3. บันทึกไฟล์

### ขั้นตอนที่ 4: เปิดใช้งาน

1. เปิดไฟล์ `index.html` ในเบราว์เซอร์
2. เว็บจะโหลดข้อมูลจาก Google Sheets อัตโนมัติ
3. เสร็จสิ้น! 🎉

## 🎨 การปรับแต่ง

### เปลี่ยนจำนวนหนังสือต่อหน้า

แก้ไขในไฟล์ `script.js` บรรทัดที่ 6:

```javascript
const ITEMS_PER_PAGE = 12; // เปลี่ยนเป็นจำนวนที่ต้องการ
```

### เปลี่ยนสีธีม

แก้ไขในไฟล์ `style.css` ที่ส่วน `:root`:

```css
:root {
    --primary-color: #1e40af;      /* สีหลัก */
    --secondary-color: #0ea5e9;    /* สีรอง */
    --accent-color: #06b6d4;       /* สีเน้น */
    /* ... */
}
```

### เปลี่ยนฟอนต์

แก้ไขในไฟล์ `index.html` ที่ `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=YOUR_FONT:wght@300;400;600;700&display=swap" rel="stylesheet">
```

และในไฟล์ `style.css`:

```css
body {
    font-family: 'YOUR_FONT', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}
```

## 🔧 การแก้ไขปัญหา

### ปัญหา: แสดง "⚙️ วิธีการตั้งค่า Google Apps Script"

**สาเหตุ:** ยังไม่ได้ใส่ Apps Script URL ใน `script.js`

**วิธีแก้:** ทำตามขั้นตอนที่ 3 ข้างบน

### ปัญหา: แสดง "เกิดข้อผิดพลาด ไม่สามารถโหลดข้อมูลได้"

**สาเหตุที่เป็นไปได้:**
1. Apps Script URL ไม่ถูกต้อง
2. ชื่อ Sheet Tab ไม่ใช่ `ebook`
3. ยังไม่ได้ Authorize Apps Script

**วิธีแก้:**
1. ตรวจสอบ URL ใน `script.js`
2. ตรวจสอบชื่อ Sheet Tab ต้องเป็น `ebook` (ตัวพิมพ์เล็ก)
3. ลอง Deploy Apps Script ใหม่

### ปัญหา: รูปปกหนังสือไม่แสดง

**สาเหตุ:** URL รูปภาพไม่ถูกต้องหรือไม่สามารถเข้าถึงได้

**วิธีแก้:** 
- ใช้ URL รูปภาพที่เป็น Public (เช่น Google Drive ที่แชร์เป็น Public)
- หรือใช้ Placeholder: `https://via.placeholder.com/280x380?text=Book+Cover`

### ปัญหา: ข้อมูลไม่อัปเดต

**สาเหตุ:** Browser Cache

**วิธีแก้:** กด `Ctrl + Shift + R` (Windows) หรือ `Cmd + Shift + R` (Mac) เพื่อ Hard Refresh

## 📊 ข้อมูลเพิ่มเติม

### Query Parameters (ขั้นสูง)

Apps Script รองรับ Query Parameters:

```
# กรองตามหมวดหมู่
https://script.google.com/.../exec?category=การละหมาด

# ค้นหา
https://script.google.com/.../exec?search=ดุอาอฺ

# รวมกัน
https://script.google.com/.../exec?category=การละหมาด&search=ตะฮายุต
```

### Keyboard Shortcuts

- `←` (Arrow Left) - หน้าก่อนหน้า
- `→` (Arrow Right) - หน้าถัดไป
- `Esc` - ปิด Modal

## 🔒 ความปลอดภัย

- ✅ ไม่มี API Key ใน Frontend
- ✅ ข้อมูลดึงผ่าน Apps Script (Backend)
- ✅ ปลอดภัยกว่าการใช้ API Key โดยตรง

## 📈 การ Deploy

### Deploy บน GitHub Pages

1. สร้าง Repository ใหม่บน GitHub
2. Upload ไฟล์ `index.html`, `style.css`, `script.js`
3. ไปที่ Settings → Pages
4. เลือก Source: main branch
5. คลิก Save
6. เข้าถึงได้ที่ `https://username.github.io/repository-name`

### Deploy บน Netlify

1. ลาก Drop โฟลเดอร์ไปที่ [Netlify Drop](https://app.netlify.com/drop)
2. เสร็จสิ้น! ได้ URL ทันที

### Deploy บน Vercel

```bash
npm install -g vercel
vercel
```

## 🎓 เทคโนโลยีที่ใช้

- HTML5
- CSS3 (Custom Properties, Grid, Flexbox, Animations)
- Vanilla JavaScript (ES6+)
- Google Apps Script
- Google Sheets API

## 📝 License

MIT License - ใช้งานได้ฟรี

## 👨‍💻 ผู้พัฒนา

Made with ❤️ by Tech for Ummah

## 🙏 ขอบคุณ

- Google Fonts (Sarabun)
- Google Sheets & Apps Script
- Placeholder.com

---

> [!TIP]
> หากมีปัญหาหรือข้อสงสัย สามารถเปิด Issue ได้เลยครับ!

**สนุกกับการใช้งาน Ebook Library! 📚✨**
